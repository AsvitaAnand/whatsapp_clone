const mongoose = require('mongoose'); mongoose.connect('mongodb://127.0.0.1:27017/whatsapp-clone').then(async () => { const User = require('./models/User'); const users = await User.find({ username: { $in: ['Arjun', 'Ashvitha', 'Meharaj', 'Margini', 'Nandini'] } }); console.log(JSON.stringify(users.map(u => ({ username: u.username, email: u.email, password: u.password })), null, 2)); process.exit(0); }).catch(console.error);
